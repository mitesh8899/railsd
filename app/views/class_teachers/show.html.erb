<% if params[:type] == "classroom" %>
  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th scope="col">Teacher Name</th>
        <th scop="col">Lecture Number</th>
      </tr>
    </thead>
    <tbody>
        <tr>
          <td><%= @classteacher.teacher.name %></td>
          <td> <%= @classteacher.lecture_number %></td>
        </tr>  
    </tbody>
  </table>
<% end %>

<% if params[:type] == "teacher" %>
  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th scope="col">Classroom Name</th>
        <th scop="col">Lecture Number</th>
      </tr>
    </thead>
    <tbody>
        <tr>
          <td><%= @classteacher.classroom.name %></td>
          <td><%= @classteacher.lecture_number %></td>
        </tr>  
    </tbody>
  </table>
<% end %>

<% if params[:type] == "lecture_number" %>
  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th scope="col">Teacher Name</th>
        <th scop="col">Classroom Name</th>
      </tr>
    </thead>
    <tbody>
        <tr>
          <td><%= @classteacher.teacher.name %></td>
          <td><%= @classteacher.classroom.name %></td>
        </tr>  
    </tbody>
  </table>
<% end %>

<%= link_to 'Back', class_teachers_path %> ||
<%= link_to 'Edit', class_teachers_path %>


<%= form_with(model: lecture) do |form| %>
  <% if lecture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lecture.errors.count, "error") %> prohibited this lecture from being saved:</h2>

      <ul>
        <% lecture.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
        <%= form.select :teacher_id, Teacher.all.collect {|p| [ p.name, p.id ] }, include_blank: true %>
        <%= form.select :classroom_id, Classroom.all.collect {|d| [ d.number, d.id ] }, include_blank: true %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number%>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.collection_select :teacher_id, Teacher.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :classroom_id %>
    <%= form.collection_select :classroom_id, Classroom.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

